---
- hosts: all
  user: "{{ deploy_user }}"
  sudo: true
  vars_files:
    - vars/defaults.yml

  roles:
    #- mysql
    - ruby-common # currently configure at ruby2.0-dev
    - chruby
    - rails
    - nginx-puma
